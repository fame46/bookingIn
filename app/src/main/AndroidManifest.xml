<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.bookingin">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_real_estate"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_real_estate"
        android:supportsRtl="true"
        android:theme="@style/Theme.BookingIn"
        android:usesCleartextTraffic="true">
        <activity android:name=".Admin.LaporanDetailActivity"></activity>
        <activity android:name=".Admin.PermintaanDetailActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".User.HomeUserActivity" />
        <activity android:name=".Admin.HomeAdmin" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>